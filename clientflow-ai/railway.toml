[build]
builder = "nixpacks"

[build.env]
NODE_ENV = "production"

[[services]]
name = "clientflow-backend"
source = "backend"
startCommand = "node server.js"

[services.env]
PORT = "5000"
NODE_ENV = "production"
